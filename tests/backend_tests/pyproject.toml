[project]
name = "crypto-sandbox-qa-backend-tests"
version = "0.0.0"
requires-python = ">=3.10"
description = "Python API tests for CryptoSandboxQA (scaffolding)."

[tool.pytest.ini_options]
pythonpath = ["."]
